// Set up empty callbacks for the user to override.
Rocket = {}
// Callback that is run whenever the user connects to the web socket server.
Rocket.onopen = function() {
  
}

// Callback that is run when the connection to the server is terminated.
Rocket.onclose = function() {
  
}

// Runs whenever the client receives a message from the server.
Rocket.onmessage = function() {
  
}

// Runs the first time a user connects to the web socket server.
Rocket.first_connect = function() {
  
}